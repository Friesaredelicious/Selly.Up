name: Keep Replit Alive

on:
  schedule:
    - cron: '*/5 * * * *'  # every 5 minutes
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Replit
        run: |
          curl -fsS https://SELLBUY.horekgorek486.repl.co/ || echo "Ping failed"
